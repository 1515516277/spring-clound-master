server:
  port: 8000
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    register-with-eureka: false #设置为注册中心
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8000/eureka/
